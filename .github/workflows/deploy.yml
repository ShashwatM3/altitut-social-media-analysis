name: Deploy

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # Wire up deployment here when ready:
      #   1. Configure AWS credentials via GitHub Secrets
      #   2. Build & push Docker image to ECR
      #   3. Deploy to ECS or run terraform apply
      - name: Placeholder
        run: echo "Wire up deployment here"
