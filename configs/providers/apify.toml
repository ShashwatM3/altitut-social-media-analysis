[provider]
name = "apify"
enabled = true
token_env = "APIFY_TOKEN"
actor_id = ""
dataset_id = ""
default_platform = "instagram"
docs_url = "https://docs.apify.com/"

setup_steps = [
  "Create or sign in to your Apify account.",
  "Create an API token in the Apify console and export it as APIFY_TOKEN.",
  "Choose the first Instagram-oriented actor or dataset workflow for competitor research.",
  "Return to the app and rerun the integration status check."
]
